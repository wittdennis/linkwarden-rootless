name: pr

on: [pull_request]

jobs:
  build:
    uses: wittdennis/pipelines/.github/workflows/container-image-build.yaml@1.13.9
    with:
      image-name: linkwarden-rootless
      create-artifacts: false
