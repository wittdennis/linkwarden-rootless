name: publish-image

on:
  release:
    types: published

jobs:
  publish:
    uses: wittdennis/pipelines/.github/workflows/container-image-publish.yaml@2.0.0
    with:
      image-name: linkwarden-rootless
    secrets:
      registry-password: ${{ secrets.DOCKER_REGISTRY_TOKEN }}
